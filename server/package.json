{
  "name": "outdeals-api",
  "version": "1.0.0",
  "private": true,
  "description": "Submissions, ranking and payments backend for the outdeals board",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "ALLOW_DEV_PAYMENTS=1 node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "check-links": "node src/checker.js"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.11",
    "express": "^5.2.1",
    "pg": "^8.16.3",
    "stripe": "^22.5.0"
  }
}
